package bebra.hack.addon.modules.render;
/*
import bebra.hack.addon.Addon;
import meteordevelopment.meteorclient.events.render.Render3DEvent;
import meteordevelopment.meteorclient.systems.modules.Module;
import meteordevelopment.meteorclient.utils.render.color.Color;
import meteordevelopment.orbit.EventHandler;
import net.minecraft.entity.player.PlayerEntity;
import net.minecraft.util.math.BlockPos;

public class Linia extends Module
{
    public Linia()
    {
        super(Addon.render, "Linia", "");
    }
    public PlayerEntity target;

    @EventHandler
    public void Render(Render3DEvent event)
    {
        float x1, y1, z1;
        float x2, y2, z2;


        int blockX = target.getBlockX();
        int blockY = target.getBlockY();
        int blockZ = target.getBlockZ();

        BlockPos blockPos = new BlockPos(blockX, blockY, blockZ);
        x1 = (float) target.getBlockX();
        y1 = (float) target.getBlockY();
        z1 = (float) target.getBlockZ();

        x2 = (float) target.getBlockX();
        y2 = (float) target.getBlockY();
        z2 = (float) target.getBlockZ();

        event.renderer.line(x1, y1, z1, x2, y2, z2, new Color(225, 0, 0, 50));
    }
}
*/
